# Fashion MNIST Classification with PyTorch

## Описание проекта

В рамках данного проекта была разработана и исследована нейронная сеть для классификации изображений одежды из набора данных Fashion MNIST с использованием библиотеки PyTorch.

Проект выполнен в качестве практической работы после изучения глав 1–10 книги *Hands-On Machine Learning with Scikit-Learn, Keras & PyTorch* (Aurélien Géron).

В ходе работы были реализованы:

* анализ и исследование датасета (EDA);
* подготовка данных и создание DataLoader;
* построение многослойного перцептрона (MLP);
* обучение модели на GPU;
* оценка качества модели на валидационной и тестовой выборках;
* анализ ошибок с помощью Confusion Matrix;
* сравнение различных гиперпараметров;
* проверка устойчивости модели к шуму.

---

## Используемый датасет

Fashion MNIST — набор изображений предметов одежды в оттенках серого.

Характеристики датасета:

* 60 000 изображений для обучения;
* 10 000 изображений для тестирования;
* размер изображения: 28 × 28 пикселей;
* 10 классов одежды.

Классы:

1. T-shirt/top
2. Trouser
3. Pullover
4. Dress
5. Coat
6. Sandal
7. Shirt
8. Sneaker
9. Bag
10. Ankle boot

---

## Архитектура модели

Используется многослойный перцептрон (MLP):

Input Layer:

* 784 нейрона (28 × 28 пикселей)

Hidden Layers:

* 300 нейронов + ReLU
* 100 нейронов + ReLU

Output Layer:

* 10 нейронов

Функция потерь:

* CrossEntropyLoss

Исследуемые оптимизаторы:

* SGD
* Adam

---

## Проведённые эксперименты

Были протестированы следующие конфигурации:

| Оптимизатор | Learning Rate |
| ----------- | ------------- |
| SGD         | 0.1           |
| SGD         | 0.01          |
| Adam        | 0.001         |

Модели сравнивались по:

* Validation Accuracy
* Test Accuracy

---

## Анализ ошибок

Для лучшей модели были построены:

* Confusion Matrix
* Примеры ошибочных предсказаний

Наиболее сложными для классификации оказались визуально похожие классы:

* Shirt
* T-shirt/top
* Pullover
* Coat

---

## Проверка устойчивости модели

Для оценки робастности модели к входным искажениям к изображениям тестовой выборки был добавлен гауссовский шум различной интенсивности.

Результаты показали постепенное снижение точности по мере увеличения уровня шума.

---

## Используемые технологии

* Python
* PyTorch
* TorchVision
* TorchMetrics
* NumPy
* Pandas
* Matplotlib
* Scikit-Learn
* Jupyter Notebook

---

## Запуск проекта

Клонировать репозиторий:

```bash
git clone <repository_url>
cd <repository_name>
```

Создать виртуальное окружение и установить зависимости:

```bash
pip install -r requirements.txt
```

Запустить Jupyter Notebook:

```bash
jupyter notebook
```

Открыть файл проекта и выполнить все ячейки.

Датасет Fashion MNIST будет автоматически загружен при первом запуске.

---

## Результаты

В ходе работы была построена модель, способная классифицировать изображения одежды с точностью более 87% на тестовой выборке.

Также было показано влияние:

* learning rate;
* выбора оптимизатора;
* уровня шума во входных данных

на качество классификации.

---

## Возможные улучшения

* использование сверточных нейронных сетей (CNN);
* применение Dropout и Batch Normalization;
* Data Augmentation;
* автоматический подбор гиперпараметров с помощью Optuna;
* использование более современных архитектур глубокого обучения.
